listen_addresses = '*'
shared_preload_libraries = 'pg_stat_statements'
pg_stat_statements.max = 10000
pg_stat_statements.track = all

# Same WAL settings as primary (harmless on a standby)
wal_level = replica
max_wal_senders = 10
wal_keep_size = 256MB
